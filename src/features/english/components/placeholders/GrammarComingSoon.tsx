import React from 'react';
import { ComingSoonPlaceholder } from './ComingSoonPlaceholder';

export const GrammarComingSoon: React.FC = () => {
    return (
        <ComingSoonPlaceholder
            title="English Grammar"
            description="Master grammar rules, syntax, and sentence correction with our upcoming comprehensive grammar module."
        />
    );
};
